[package]
name = "exec"
version = "0.1.0"
edition = "2021"

[dependencies]
exec-core = { path="../exec-core" }
exec-executor = { path="../exec-executor" }

[dev-dependencies]
exec-test = { path="../exec-test" }
futures = "0.3"
